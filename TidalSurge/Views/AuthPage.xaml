<Page
    x:Class="TidalSurge.Views.AuthPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource BgBaseBrush}">
        <Grid>
            <Border Width="520" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Background="{StaticResource PanelGlassBrush}" BorderBrush="{StaticResource PanelStrokeBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="24">
                <StackPanel Spacing="16">
                    <TextBlock Text="Activate License" Style="{StaticResource HeaderText}"/>
                    <TextBlock Text="Enter your license key" Style="{StaticResource BodyText}"/>
                    <TextBox x:Name="LicenseInput" PlaceholderText="XXXX-XXXX-XXXX-XXXX" Style="{StaticResource InputStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Button x:Name="ActivateButton" Content="Activate" Click="OnActivateClick" Style="{StaticResource PrimaryButtonStyle}"/>
                        <ProgressRing x:Name="ActivateSpinner" Width="20" Height="20" IsActive="False" Visibility="Collapsed"/>
                    </StackPanel>
                    <TextBlock x:Name="StatusText" Style="{StaticResource CaptionText}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>

